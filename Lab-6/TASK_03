#include <stdio.h>
 int main()
{
    int code;
    do
    {
        printf("Enter access code: ");
        scanf("%d",&code);
        if(code!=6782)
        {
            printf("Wrong code.Try again\n");
        }

    } while(code!=6782);

    printf("Access granted.Door unlocked\n");
    return 0;
}
